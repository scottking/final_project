<li>
  <%= link_to board.name, board_path(board) %> | Created by: 
	<% if board.user != nil %>
		<%= board.user.name %>
	<% else %>
		old user
	<% end %>
  <% if current_user?(board.user) %>
    | <%= link_to "delete", board, method: :delete, 
								  data: { confirm: "You sure?" } %>
  
  <% end %>
  
  
</li>
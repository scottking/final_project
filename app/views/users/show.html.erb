<%= provide(:title, @user.name) %>
<%= render 'shared/menu' %>

<div class="row">
  <aside class="span4">
    <section>
      <h1>
        
		<%= gravatar_for @user %>
		<ol>
          <li><%= @user.name %></li>
		  <li><%= @user.email %></li>
		</ol>
      </h1>

    </section>
    <section>
    
    </section>
  </aside>
  <div class="span8">
    
	<%= button_to "Create board", new_board_path, :method => :get %>

    
  </div>
  
  <% if current_user?(@user) %><%= link_to "Edit profile", edit_user_path %><% end %>
</div>